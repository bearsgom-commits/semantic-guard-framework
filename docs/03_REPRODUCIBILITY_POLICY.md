# Reproducibility Policy

- All experiments are executable via Makefile.
- No manual metric computation is allowed.
- Git commit hash is logged.
- Python version and pip freeze are recorded.
- All randomness controlled by seed ∈ {7,13,42}.